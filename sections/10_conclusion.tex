% =============================================================================
%  Conclusion
%
%  Hints
%  -----
%  - Do NOT introduce new arguments, claims, or literature here.
%    The conclusion synthesises; it does not extend.
%  - Open with a 1–2 sentence frame reminding the reader of the three fields
%    covered and the methodology used.
%  - Summary of findings: 3–5 numbered, bolded findings. Each finding should
%    be a defensible claim grounded in the reviewed literature, not a claim
%    about your own future work.
%  - Contributions: state the primary contribution of the SLR as a review
%    (the gap map, the concept matrix) — not as a system proposal.
%  - Threats to validity: four named types — construct, internal, external,
%    conclusion validity. Each requires a specific mitigation statement.
%  - Limitations and future work: 3–4 concrete limitations, each leading
%    to a specific future work direction. Be honest about what you cannot claim.
% =============================================================================

\section{Conclusion}
\label{sec:conclusion}

This systematic literature review spans [N] intersecting fields that together
motivate a novel research direction: [list covered fields with section refs].
The review followed the PRISMA 2020 guidelines \citep{Page2021}, the eight-step
process of \citet{XiaoWatson2019}, and the snowballing procedure of
\citet{Wohlin2014}, yielding a final corpus of [N] papers assembled from [N]
seed papers and [N] thematic pre-validated groups.

% =============================================================================
\subsection{Summary of Findings}
\label{sec:conclusion:summary}

% Hints:
% - 3–5 findings as numbered, bolded items.
% - Each finding should be a positive claim about what the literature
%   establishes — not a restatement of what is missing.
% - The gaps are stated in the final bullet ("X has Y but lacks Z").

The review established [N] key findings:

\begin{enumerate}
  \item \textbf{[Finding 1 — positive claim].}
    [REPLACE: One paragraph elaborating what the literature confirms. End by
    noting the gap: "No reviewed work has operationalised this into X."]

  \item \textbf{[Finding 2 — positive claim].}
    [REPLACE: Elaborate as above.]

  \item \textbf{[Finding 3 — positive claim].}
    [REPLACE: Elaborate as above.]

  \item \textbf{[Finding 4 — gap claim].}
    [REPLACE: State what a body of work contributes toward the research
    direction AND what it does not address.]
\end{enumerate}

% =============================================================================
\subsection{Research Gap Identification and Systematisation}
\label{sec:conclusion:contributions}

% Hints:
% - State the primary contribution of this SLR as a piece of scholarship.
% - Name the key artefacts: the concept matrix (Table~\ref{tab:concept_matrix})
%   and the gap map (Table~\ref{tab:gap_map}).
% - Use hedged language: "to the best of the author's knowledge".

The primary contribution of this review is the identification and structured
characterisation of a compound research gap at the intersection of [N] active
fields. The concept matrix (Table~\ref{tab:concept_matrix}) shows that, to the
best of the author's knowledge, no existing [system / method / framework]
simultaneously addresses all [N] design dimensions of [your research direction].
The research gap map (Table~\ref{tab:gap_map}) decomposes this compound gap into
[N] open research questions across four quadrants, each demanding a different
type of research contribution.

[REPLACE: Characterise what the systematisation of [N] papers achieves —
e.g., identifying a theoretical gap, establishing an evidence base for future
empirical work, identifying a missing intersection between fields.]

% =============================================================================
\subsection{Threats to Validity}
\label{sec:conclusion:threats}

% Hints:
% - Four types of validity must be addressed. Use \textit{} for the type names.
% - Each threat requires (a) naming the risk and (b) stating the mitigation.
% - Do not understate threats — reviewers respect honest assessment.

Several threats to the validity of this review are acknowledged.

\textit{Construct validity.}
[REPLACE: Name the construct validity threat — typically that the dimensions in
the concept matrix were derived inductively and a different set could yield a
different gap profile. State the mitigation (e.g., rationale for each dimension
is documented in Section~\ref{sec:synthesis_gap:matrix}).]

\textit{Internal validity.}
[REPLACE: Name the internal validity threat — typically single-author selection
bias. State mitigation measures (keyword scoring, structured term sets,
AI-assisted consistency checks, logged decisions).]

\textit{External validity.}
[REPLACE: Name the external validity threat — typically language bias
(English-only corpus) or domain focus (NLP-only). State what this means for
generalisability.]

\textit{Conclusion validity.}
[REPLACE: Name the conclusion validity threat — typically one snowballing wave
with informally assessed saturation, or the rapidly evolving field (papers
after the search date may address identified gaps). State the search date
explicitly.]

% =============================================================================
\subsection{Limitations and Future Work}
\label{sec:conclusion:limitations}

% Hints:
% - 3–4 concrete limitations, each leading to a specific future direction.
% - Limitations are structural properties of the review (not personal failings).
% - Future work directions should derive directly from the gap map in
%   Table~\ref{tab:gap_map} — one direction per gap quadrant is a natural fit.
% - Do not include limitations that are trivially fixable (e.g., "we could
%   have read more papers"). Name only those that constrain the conclusions.

[REPLACE: Limitation 1 — e.g., all findings rest on literature evidence and
the author's analytical validation; no empirical work yet validates the open
theoretical questions identified in the gap map.]

[REPLACE: Limitation 2 — e.g., English-only corpus limits generalisability to
multilingual settings.]

[REPLACE: Limitation 3 — e.g., incentive / trust / security dimensions were
excluded from the analysis; state why and acknowledge the significance.]

Future work should prioritise [N] directions emerging directly from the gap map.
First, [REPLACE: direction from the empirical quadrant, citing relevant prior
work \citep{ExampleFuture2020}]. Second, [REPLACE: direction from the
algorithmic quadrant]. Third, [REPLACE: direction from the systems quadrant].
Fourth, [REPLACE: direction from the conceptual quadrant].
